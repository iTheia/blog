import faker from 'faker'
import { User } from '../schemas'

module.exports = function users (quantity){
    let user = []

    for (let index = 0; index < quantity; index++) {
        const element = {
            name:''
        }
    }

} 